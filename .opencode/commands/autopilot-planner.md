---
description: "Run the autopilot planner role"
---

You are running the OpenCode `autopilot-planner` command.

Use the built-in `planner` role.
Favor clarifying ambiguity and producing a plan before code changes start.
Use the effective `autopilot` config to determine which model and prompt to apply.
